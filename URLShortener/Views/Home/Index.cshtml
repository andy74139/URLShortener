@model IEnumerable<ShortenedUrl>
@{
    ViewData["Title"] = "Home Page";
}

<form action="/CreateShortenedUrl" method="post">
    <input name="url" type="text" />
    <button name="submit">Generate Shortened URL</button>

</form>
<table border="1">
    <thead>
    </thead>
    <tr>
        <th>Shortened</th>
        <th>Original</th>
        <th>Most recent use</th>
        <th>Detail</th>
    </tr>
    @foreach (var shortenedUrl in Model)
    {
        <tr>
            <td>@shortenedUrl.ShortenedPath</td>
            <td><a href="@shortenedUrl.TargetUrl">@shortenedUrl.TargetUrl</a></td>
            <td>@shortenedUrl.LastUseTime</td>
            <td><img src="~/images/2000px-Icon_Stats_active.svg.png" width="25px" /></td>
        </tr>
    }

</table>

